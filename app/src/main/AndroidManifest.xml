<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.techtown.club">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Club.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".post.GroupWriteActivity"></activity>
        <activity android:name=".post.groupAdd" />
        <activity android:name=".post.GroupActivity" />
        <activity android:name=".notice.NoticeWriteActivity" />
        <activity android:name=".receipt.MoneyAdd" />
        <activity
            android:name=".receipt.DetailMoneyActivity"
            android:exported="true"
            android:label="@string/title_activity_detail_money"
            android:theme="@style/Theme.Club.NoActionBar.NoActionBar" />
        <activity android:name=".register.JoinClubActivity" />
        <activity android:name=".OpenClubActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".register.RegisterActivity" />
        <activity android:name=".register.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>