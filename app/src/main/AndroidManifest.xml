<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.techtown.club">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Club.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".GroupWriteActivity"></activity>
        <activity android:name=".groupAdd" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".NoticeWriteActivity" />
        <activity android:name=".MoneyAdd" />
        <activity
            android:name=".DetailMoneyActivity"
            android:exported="true"
            android:label="@string/title_activity_detail_money"
            android:theme="@style/Theme.Club.NoActionBar.NoActionBar" />
        <activity android:name=".RegisterActivity2" />
        <activity android:name=".RegisterActivity1" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>